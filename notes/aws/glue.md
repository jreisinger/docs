AWS Glue
- serverless data integration service
- analytics, ML and app. dev.

AWS Glue Schema
- describes the structure of data
- is ACS it's not used by EventBridge but by consumer (lambda)
  and by tests to keep producers and consumers aligned

AWS Glue Schema registry
- centrally discover, control, and evolve data stream schemas
