* a generic name for kubernetes configuration file that holds cluster configuration
* it's in YAML format
* it's used by `kubectl`
* it is the one or more files defined in `KUBECONFIG`
* if `KUBECONFIG` is unset `~/.kube/config` is used
